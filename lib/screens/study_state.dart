import 'package:flutter/material.dart';

class StudyState extends StatelessWidget {
  const StudyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('state'),
      ),
    );
  }
}
